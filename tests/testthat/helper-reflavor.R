# Shared by the correspondence tests: the multiplicative reflavoring
# constants, read from the same object data-raw/reflavor_datasets.R applies
# (never re-typed here) so the tests catch any drift between the two.
K <- behdslabs:::.reflavor_k
