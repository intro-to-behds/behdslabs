% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitness_app_downloads_by_state.R
\docType{data}
\name{fitness_app_downloads_by_state}
\alias{fitness_app_downloads_by_state}
\title{Fitness-app downloads by US state, app category, and year}
\format{
An object of class \code{"data.frame"}.
}
\source{
Original \code{dslabs::us_contagious_diseases} values (Irizarry &
  Gill). The 7 disease categories are relabeled 1:1 to 7 fitness-app
  categories; \code{count} -> \code{downloads}; \code{weeks_reporting} ->
  \code{weeks_tracked}. Numeric values are otherwise unchanged.
}
\usage{
fitness_app_downloads_by_state
}
\description{
Weekly-tracked download counts for seven fitness/wellness app categories,
by US state and year, alongside state population -- used to illustrate
rate computation, adjusting for partial-year tracking coverage, and
regional/temporal comparison.
}
\details{
\itemize{
  \item app_category. One of seven fitness-app categories (e.g. Guided
    Meditation, Step Tracking).
  \item state. Name of state.
  \item year. Year.
  \item weeks_tracked. Number of weeks in the year for which download
    data was actually tracked (data completeness varies by state/year).
  \item downloads. Number of app downloads reported.
  \item population. Population of state.
}


**Teaching connection:** app adoption follows epidemic-like dynamics --
rapid growth after a launch or benefit-program event, a plateau at
saturation, and sometimes a sharp decline after a competing product or
a reputation event. The rate-computation (downloads adjusted for
population and tracking coverage), time-series, and event-annotation
workflow used on this dataset applies directly to real app-store
download/uninstall data around a marketing campaign or feature launch.
}
\note{
These are not real app-download data. They are the original
  \code{dslabs::us_contagious_diseases} values, relabeled. A synthetic or
  real, ethically-sourced replacement is planned for a future release
  (see \code{BDS_development_plan.md} Phase 2).
}
\examples{
fitness_app_downloads_by_state |> head()

# Rate per 10,000 population, adjusted for partial-year tracking --
# the same adjustment needed for any state/year panel where reporting
# coverage is incomplete.
library(dplyr)
fitness_app_downloads_by_state |>
  filter(app_category == "Guided Meditation") |>
  mutate(rate = downloads / population * 10000 * 52 / weeks_tracked) |>
  arrange(desc(rate)) |>
  head()
}
\keyword{datasets}
